# Code style

The project uses C++17, four-space continuation indentation as encoded by `.clang-format`, GPL-3.0-or-later SPDX headers, explicit typed errors, immutable value construction, and no ambient authority. Public behavior is documented where it is declared. Shell contracts are POSIX `sh` and fail closed.
